# UI仕様書 — パソコン初心者おすすめセット

> 対象ページ: https://deskgear-nu.vercel.app/ （トップページ）  
> 解析日時: 2026-09-18  
> 目的: 「パソコン初心者おすすめセット」セクションおよびトップページ全体のレイアウト構成を記録し、再実装・改修の参考仕様とする。

---

## 1. ページ全体構成

### 1-1. レイアウト骨格

```
┌────────────────────────────────────────────────────────────┐
│ ヘッダー（ロゴ + グローバルナビ）                          │
│ 最終更新日バー                                              │
├────────────────────────────────────────────────────────────┤
│ [A] パソコン初心者おすすめセット セクション                │
│     ├── セクションヘッダー（青帯）                         │
│     ├── ヒーロー画像（デスクセットアップ）                  │
│     └── 商品カードグリッド（3カラム）                      │
│         └── 免責・補足テキスト + 「他のノートPCを探す」    │
├────────────────────────────────────────────────────────────┤
│ [B] カテゴリ セクション                                    │
├────────────────────────────────────────────────────────────┤
│ [C] 絞り込み検索パネル                                     │
├────────────────────────────────────────────────────────────┤
│ [D] 総合おすすめランキング ベスト10                         │
└────────────────────────────────────────────────────────────┘
```

### 1-2. カラースキーム（ライトテーマ）

| トークン | 値 | 用途 |
|---|---|---|
| 背景 | `#ffffff` / `#f8f9fa` | ページ背景、カード背景 |
| プライマリ | `#2a52be`（コバルトブルー系） | セクションヘッダー帯、バッジ、ボタン |
| アクセント（楽天） | `#cc0000`（レッド） | 「楽天で見る」CTAボタン |
| テキスト（メイン） | `#111827` | 商品タイトル、価格 |
| テキスト（サブ） | `#6b7280` | メーカー名、補足テキスト |
| テキスト（注記） | `#3b82f6`（青） | 注意書き（「※ 価格は…」） |
| ボーダー | `#e5e7eb` | カード外枠、区切り |
| 評価スター | `#f59e0b`（アンバー） | ★評価アイコン |

### 1-3. タイポグラフィ

| 要素 | サイズ感 | ウェイト | 備考 |
|---|---|---|---|
| セクション見出し（青帯内） | `text-2xl` 相当（約24px） | Bold | 白文字 |
| セクション見出し（白地） | `text-xl` 相当（約20px） | Bold | 例：「カテゴリ」「総合おすすめランキング ベスト10」 |
| 商品タイトル | `text-base`〜`text-sm` 相当 | Semi-Bold | 2行程度で折り返し |
| メーカー名 | `text-sm` | Normal | グレー |
| 価格 | `text-xl`〜`text-2xl` 相当 | Bold | 例：「¥149,800」 |
| 補足テキスト（1行コメント） | `text-sm` | Normal | イタリック、グレー |

---

## 2. ヘッダー

| 要素 | 仕様 |
|---|---|
| ロゴ | ⚙️ アイコン + 「DESKGEAR」テキスト（ブルー系） |
| ナビ | 「トップ」「商品検索」の2リンク（右寄せ） |
| 背景 | 白（`#ffffff`）、下ボーダー薄グレー |
| 最終更新日バー | `🕐 最終更新日：2026年9月18日` （小さいグレーテキスト、ヘッダー直下） |

---

## 3. [A] パソコン初心者おすすめセット セクション

このセクションがトップページ最上部（ヘッダー直下）に配置される最重要コンテンツ。

### 3-1. セクションヘッダー帯

```
┌────────────────────────────────────────────────────────────────┐
│ 🛍 [初心者向けセット]  パソコン初心者おすすめセット            │
│                        とりあえずこれで始めよう — 必要なものを │
│                        最安値水準でまとめました                  │
└────────────────────────────────────────────────────────────────┘
```

| 要素 | 仕様 |
|---|---|
| 帯の背景色 | コバルトブルー（`#2a52be` 系） |
| バッジ | 「🛍 初心者向けセット」— 白文字・青背景の小バッジ、左上配置 |
| メインタイトル | 「パソコン初心者おすすめセット」— 白・Bold・約24px |
| サブタイトル | 「とりあえずこれで始めよう — 必要なものを最安値水準でまとめました」— 白・Normal・小さめ |
| 角丸 | `rounded-lg` 相当（上辺のみ丸め） |
| パディング | `px-6 py-4` 相当 |

### 3-2. ヒーロー画像

| 要素 | 仕様 |
|---|---|
| 配置 | セクションヘッダー帯の直下、フル幅（カード外枠内） |
| 内容 | デスクセットアップ写真（ノートPC + モニター + マウス + 観葉植物） |
| 高さ | 約 450px（レスポンシブ対応） |
| Object-fit | `cover` |
| 角丸 | なし（上辺は帯に接続、下辺はカードグリッドに接続） |

### 3-3. 商品カードグリッド

セクション全体を包む外枠カード（薄グレーボーダー、`rounded-lg`）内に3カラムのグリッドで配置。

#### グリッドレイアウト

| ブレークポイント | カラム数 |
|---|---|
| モバイル（< 768px） | 1カラム |
| タブレット（768px〜） | 2カラム（推定） |
| デスクトップ（1024px〜） | 3カラム |

#### 個別カードの構造

```
┌───────────────────────────────────┐
│ [カテゴリバッジ]                   │  ← 左上オーバーレイ
│                                   │
│  ┌─────────────────────────────┐  │
│  │      商品画像               │  │  ← アスペクト比 4:3 程度
│  │      (グレー背景)           │  │
│  └─────────────────────────────┘  │
│                                   │
│  商品タイトル（太字）              │
│  メーカー名（グレー小テキスト）    │
│  補足コメント（イタリック・グレー）│
│                                   │
│  ¥価格（大・太字）                │
│                                   │
│ [楽天で見る]（赤ボタン・フル幅）   │
└───────────────────────────────────┘
```

#### カードの詳細仕様

| 要素 | 仕様 |
|---|---|
| カード背景 | 白（`#ffffff`） |
| カード外枠 | 薄グレーボーダー（`border border-gray-200`相当） |
| 角丸 | `rounded-lg` |
| パディング | 画像なし、テキスト部は `p-3`〜`p-4` 相当 |
| 画像エリア | グレー背景（`bg-gray-100`相当）、商品画像を `object-contain` で表示 |
| カテゴリバッジ | 画像の左上にオーバーレイ。背景: プライマリブルー、白テキスト、`rounded-full`、`text-xs`、`px-3 py-1` |
| 商品タイトル | `font-semibold`、`text-sm`〜`text-base`、2行程度 |
| メーカー名 | `text-sm`、`text-gray-500` |
| 補足コメント | `text-xs`〜`text-sm`、`text-gray-400`、イタリック |
| 価格 | `text-xl`〜`text-2xl`、`font-bold`、`¥` プレフィックス、カンマ区切り |
| CTAボタン | フル幅（`w-full`）、`py-2.5` 程度、赤背景（`#cc0000`）、白テキスト、`font-bold`、`rounded-md` |

#### 表示商品（2026-09-18 時点）

| # | カテゴリバッジ | 商品名 | メーカー | 補足コメント | 価格 | CTA |
|---|---|---|---|---|---|---|
| 1 | 「ノートパソコン」 | ASUS Vivobook 14 X1407CA-U5165BU | ASUS | Webカメラ内蔵・顔認証対応でビデオ会議もすぐ使えます | ¥149,800 | 楽天で見る |
| 2 | 「マウス」 | ロジクール ワイヤレスマウス M240GRda | ロジクール | ※ ノートPCにマウスは付属しません | ¥1,780 | 楽天で見る |
| 3 | 「モニター（任意）」 | Dell E2425HM 23.8インチ モニター | Dell | 画面が広がると、作業効率が格段に変わります | ¥13,699 | 楽天で見る |

### 3-4. フッターエリア（セクション内）

セクションの最下部（カードグリッドの下）に補足テキストと外部リンクを配置。

| 要素 | 仕様 |
|---|---|
| 免責・補足テキスト | `text-xs`、`text-blue-500`（青）、左寄せ、2行 |
| 外部リンク | 「他のノートパソコンを探す ↗」、右寄せ、青テキスト、`text-sm`、外部リンクアイコン付き |

文言例:
- 「※ 価格は楽天市場の参考価格です。変動する場合があります。」
- 「※ モニターはお好みで追加してください（なくてもノートPCで完結します）。」

---

## 4. [B] カテゴリ セクション

### 4-1. セクション見出し

| 要素 | 仕様 |
|---|---|
| テキスト | 「カテゴリ」 |
| スタイル | `text-xl`、`font-bold`、黒テキスト |

### 4-2. カテゴリカードグリッド

| ブレークポイント | カラム数 |
|---|---|
| モバイル | 3カラム |
| タブレット | 4〜5カラム |
| デスクトップ | 6カラム |

#### カテゴリカードの構造

```
┌───────────────┐
│               │
│    [アイコン]  │   ← lucide-react アイコン（約24px）
│               │
│  カテゴリ名   │   ← text-sm、中央揃え
│               │
└───────────────┘
```

| 要素 | 仕様 |
|---|---|
| カード背景 | 白 |
| 外枠 | `border border-gray-200` 相当 |
| 角丸 | `rounded-lg` |
| アイコン色 | `text-blue-600` 程度 |

#### カテゴリ一覧（2026-09-18 時点）

1行目: デスクトップパソコン / ノートパソコン / モニター / キーボード / マウス / ヘッドセット  
2行目: Webカメラ / セキュリティ / PCアクセサリ

---

## 5. [C] 絞り込み検索パネル

### 5-1. フォーム要素（3カラム + ボタン）

| フィールド | 種類 | 備考 |
|---|---|---|
| カテゴリ | Select | すべて / 各カテゴリ |
| メーカー | Select | カテゴリ連動で動的変化 |
| 並び替え | Select | 価格が高い順（デフォルト）/ 価格が安い順 / 評価が高い順 / 発売日が新しい順 |
| ボタン | Button | 「この条件で検索」— プライマリブルー |

---

## 6. [D] 総合おすすめランキング ベスト10

### 6-1. 商品カード（3カラムグリッド）

#### ランキングカードの構造

```
┌───────────────────────────────────┐
│ [①]                               │  ← 順位バッジ（左上丸型）
│  ┌─────────────────────────────┐  │
│  │      商品画像               │  │
│  └─────────────────────────────┘  │
│  商品タイトル（太字）              │
│  メーカー名（グレー）              │
│  ★ 評価スコア          ¥価格     │
│ [楽天で見る]（赤ボタン・フル幅）   │
└───────────────────────────────────┘
```

| 要素 | 仕様 |
|---|---|
| 順位バッジ | 円形（`rounded-full`）、背景: プライマリブルー、白テキスト、`w-7 h-7` 程度 |
| 評価スター | `★`（filled）、`text-amber-400` |
| 評価スコア | 数値（例: `5.0`）、`text-sm` |
| 価格 | `font-bold`、右寄せ、`text-lg` 程度 |

---

## 7. 実装メモ

### 7-1. コンテナ幅

全体を `max-w-4xl`〜`max-w-6xl` の中央揃えコンテナで包む（実測: 約 1024px）。

### 7-2. セクション配置の意図

- 「パソコン初心者おすすめセット」をファーストビューに置くことで、来訪即コンバージョンを狙う設計
- ヒーロー画像で完成形デスク環境を見せ、購入意欲を喚起
- 任意品（モニター）を明示することで、ミニマムな購入ハードルを設定しつつアップセルを誘導

### 7-3. CTA設計

- 楽天リンクのみ表示（Amazonリンク非表示中）
- ボタンは `w-full` で視認性・タップ面積を確保
- 楽天レッド（`#cc0000` 系）で楽天ブランドカラーと統一

---

## 8. 改修候補（解析時点の観察）

- [ ] 「モニター（任意）」バッジを他色（グレー・薄青など）で視覚的に区別
- [ ] セット合計金額の表示（「3点合計: ¥164,279〜」）でユーザーの意思決定を支援
- [ ] ヒーロー画像へのテキストオーバーレイ追加
- [ ] モバイル時のヒーロー画像の高さ調整
